[project]
name = "relativistic-engine"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "numpy>=2.5.2",
]

[project.scripts]
relativistic-engine = "relativistic_engine.experiment:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
